{
  "annotations": {
    "list": []
  },
  "panels": [
    {
      "type": "timeseries",
      "title": "Latencia pago (ms)",
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(payment_initiation_duration_ms_bucket[5m])) by (le))", "datasource": "Prometheus" },
        { "expr": "histogram_quantile(0.95, sum(rate(payment_confirmation_duration_ms_bucket[5m])) by (le))", "datasource": "Prometheus" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 }
    },
    {
      "type": "timeseries",
      "title": "Latencia inscripción torneos (ms)",
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(tournament_join_latency_ms_bucket[5m])) by (le))", "datasource": "Prometheus" },
        { "expr": "histogram_quantile(0.99, sum(rate(tournament_join_latency_ms_bucket[5m])) by (le))", "datasource": "Prometheus" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 }
    },
    {
      "type": "timeseries",
      "title": "Errores por flujo",
      "targets": [
        { "expr": "sum(increase(workflow_errors_total[5m])) by (stage)", "datasource": "Prometheus" },
        { "expr": "sum(increase(api_failures_total[5m])) by (path, code)", "datasource": "Prometheus" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 }
    },
    {
      "type": "stat",
      "title": "Tamaño cola notificaciones",
      "targets": [
        { "expr": "queue_size{queue=\"notification_dispatch\"}", "datasource": "Prometheus" }
      ],
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 8 }
    }
  ],
  "refresh": "10s",
  "schemaVersion": 38,
  "title": "Miniappworld Observabilidad",
  "version": 1
}
